<% provide(:title, 'Edit room' ) %>

<div style="text-align:right;"><%= link_to "Back to rooms page", Rooms_path %></div>

<h1> Edit <%= @room.name %></h1>

<%= form_for( @room, html: { multipart: true } ) do |f| %>
	<%= render 'fields', f: f %>
	<%= f.submit "Update room", class: "btn btn-large" %>
<% end %>