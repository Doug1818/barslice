<header class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container">
			<%= link_to "BarSlice", root_path, id: "logo" %>
			<div id="session">
				<% if user_signed_in? %>
			        Signed in as <%= current_user.email %>. Not you?
			        <%= link_to 'Sign out', destroy_user_session_path, method: "delete" %>
		        <% elsif bar_signed_in? %>
			        Signed in as <%= current_bar.email %>. Not you?
			        <%= link_to 'Sign out', destroy_bar_session_path, method: "delete" %>
			    <% else %>
			        <span class="dropdown">
				        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Sign up<b class="caret"></b>
						</a>
						<ul class="dropdown-menu">
							<li><%= link_to "As user", new_user_registration_path %></li>
							<li><%= link_to "As bar", new_bar_registration_path %></li>
						</ul>
					</span>
			        or
			        <%= link_to 'Sign in', new_user_session_path %>
			    <% end %>
		    </div>
		</div>
	</div>
</header>