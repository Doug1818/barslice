<div style="text-align:right;"><%= link_to "Back to Reservation Policy page", respolicies_path %></div>
<%= form_for @respolicy, :html => { :multipart => true } do |f| %>
  	<%= render 'respolicies/fields', f: f %>
  	<%= f.submit "Submit", class: "btn btn-large button" %>
<% end %>