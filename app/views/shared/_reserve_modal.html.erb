<a class="btn btn-small btn-info reserve" data-toggle="modal" href="#reserveModal-<%= room.id %>" >Reserve</a>

<div class="modal hide" id="reserveModal-<%= room.id %>">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal">×</button>
    <h3>This bar does not yet participate in BarSlice reservations</h3>
  </div>
  <div class="modal-body">
    <p>To place a reservation with <%= object.name %>, see reservation guidelines at 
      <%= link_to object.website, "http://#{object.website}", target: "_blank" %></p>

    <p>If you would like to be able to make online reservation with <%= object.name %> in the future, please let them know that you found them through BarSlice and click <%= link_to 'here', '#' %> to let us know that we should follow up with them.</p>

    <p>Good luck with your event!</p>
  </div>
  <div class="modal-footer">
    <a href="#" class="btn" data-dismiss="modal">Close</a>
  </div>
</div>

<script type="text/javascript">
$('#reserveModal-<%= room.id %>').css({
          'margin-top': function () {
            return -($(this).height() / 2);
          }
        })
</script>